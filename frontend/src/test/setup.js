// Importar los matchers de jest-dom para extender `expect` de Vitest.
// Esto nos permite usar afirmaciones como .toBeInTheDocument(), .toHaveTextContent(), etc.
// que hacen los tests más legibles y centrados en el DOM.
import "@testing-library/jest-dom";
